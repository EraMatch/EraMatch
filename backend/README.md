# EraMatch Backend

FastAPI backend for EraMatch recruitment platform.

## Setup

```bash
cd backend
pip install -e .
uvicorn app.main:app --reload
```

## API Docs

- Swagger: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Environment

Copy `.env.example` to `.env` and fill in your values.
